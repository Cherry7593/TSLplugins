# TSLplugins - 用户与开发者指南

[![Kotlin](https://img.shields.io/badge/Kotlin-1.9.21-blue.svg)](https://kotlinlang.org)
[![Paper](https://img.shields.io/badge/Paper-1.21.8-green.svg)](https://papermc.io)
[![Folia](https://img.shields.io/badge/Folia-Supported-brightgreen.svg)](https://papermc.io/software/folia)

> TSL服务器的Minecraft插件集合 - 完全使用 Kotlin 编写，支持 Folia 1.21.8

---

## 📋 目录

1. [项目概述](#项目概述)
2. [快速开始](#快速开始)
3. [功能模块](#功能模块)
4. [配置文件详解](#配置文件详解)
5. [命令与权限](#命令与权限)
6. [PlaceholderAPI 扩展](#placeholderapi-扩展)
7. [常见问题](#常见问题)
8. [二次开发指南](#二次开发指南)

---

## 项目概述

### 基本信息

- **名称**: TSLplugins
- **版本**: 1.0
- **语言**: Kotlin 1.9.21
- **服务器**: Paper/Folia 1.21.8
- **作者**: Zvbj

### 核心特性

- ✅ **模块化设计** - 每个功能独立，易于维护
- ✅ **Kotlin 实现** - 简洁、安全、高效
- ✅ **Folia 兼容** - 完美支持多线程服务器
- ✅ **配置热重载** - 无需重启即可应用更改
- ✅ **智能配置更新** - 自动合并新版本配置项

### 为什么使用 Kotlin？

- **空安全** - 编译时避免 NullPointerException
- **简洁语法** - 代码量减少约 30%
- **数据类** - 自动生成 equals/hashCode/toString
- **Lambda 表达式** - 更优雅的回调处理
- **字符串模板** - 清晰的消息格式化

---

## 快速开始

### 安装步骤

1. **下载插件**
   - 将 `TSLplugins-1.0.jar` 放入服务器的 `plugins` 目录

2. **安装依赖**
   - [LuckPerms](https://luckperms.net/) - 权限管理（必需）
   - [PlaceholderAPI](https://www.spigotmc.org/resources/6245/) - 变量支持（可选）
   - [ProtocolLib](https://www.spigotmc.org/resources/1997/) - 协议支持（可选，用于维护模式）

3. **启动服务器**
   - 插件会自动生成配置文件到 `plugins/TSLplugins/`

4. **验证安装**
   ```
   [TSLplugins] TSL插件启动成功！
   [TSLplugins] 成功加载 X 个命令别名
   [TSLplugins] 维护模式状态: 未启用
   ```

### 基础配置

**编辑 aliases.yml**（命令别名）：
```yaml
aliases:
  - "t:tpa"          # /t → /tpa
  - "传送:tpa"       # 支持中文
  - "h:home"         # /h → /home
```

**编辑 config.yml**（根据需要调整）：
- 启用/禁用功能模块（所有功能都有独立开关）
- 自定义消息和提示
- 调整权限检测规则

**功能开关**（所有模块默认启用，可按需禁用）：
```yaml
# 所有功能都有统一的开关格式
feature-name:
  enabled: true  # 设为 false 可禁用该功能
```

**重载配置**：
```
/tsl reload
```

---

## 功能模块

> 💡 **提示**：每个功能模块都可以通过配置文件独立启用/禁用，无需重启服务器。

### 1. 命令别名系统 ⭐

为现有命令创建自定义别名，支持完整的 Tab 补全。

**配置开关**：`alias.enabled`

#### 核心特性

- ✅ **动态命令注册** - 别名显示为白色（不是红色）
- ✅ **Tab 补全** - 自动补全玩家名和参数
- ✅ **中文支持** - 完美支持中文别名
- ✅ **子命令简化** - 支持复杂命令映射
- ✅ **实时重载** - 修改后立即生效

#### 配置示例

**aliases.yml**
```yaml
aliases:
  # 基础别名
  - "t:tpa"
  - "ta:tpaccept"
  - "td:tpadeny"
  
  # 中文别名
  - "传送:tpa"
  - "回家:home"
  - "设置家:sethome"
  
  # 子命令简化
  - "stp tp:teleport"      # /stp tp x y z → /teleport x y z
  - "gms:gamemode survival"
  - "gmc:gamemode creative"
```

#### 使用方法

```bash
# 使用别名
/t PlayerName         # → /tpa PlayerName
/传送 Steve           # → /tpa Steve

# 使用子命令别名
/stp tp 100 64 100    # → /teleport 100 64 100

# Tab 补全
/t [Tab]              # 显示在线玩家列表
```

#### 管理命令

| 命令 | 说明 | 权限 |
|------|------|------|
| `/tsl aliasreload` | 重载别名配置 | `tsl.alias.reload` |

---

### 2. 维护模式 ⭐

全功能的服务器维护模式，支持白名单、自定义 MOTD 和踢出消息。

**配置开关**：`maintenance.enabled`

#### 核心特性

- ✅ **一键切换** - 快速开启/关闭维护模式
- ✅ **白名单系统** - 管理维护期间可进入的玩家
- ✅ **登录前拦截** - 避免区块加载
- ✅ **自定义 MOTD** - 在服务器列表显示维护信息
- ✅ **版本栏修改** - 显示"维护中"标识
- ✅ **假玩家数** - 自定义人数显示
- ✅ **状态持久化** - 重启后保持维护状态

#### 使用方法

```bash
# 开启维护模式
/tsl maintenance on

# 添加白名单玩家（可在维护时进入）
/tsl maintenance add PlayerName

# 查看白名单
/tsl maintenance whitelist

# 关闭维护模式
/tsl maintenance off

# 移除白名单
/tsl maintenance remove PlayerName
```

#### 配置示例

**config.yml - maintenance 部分**
```yaml
maintenance:
  messages:
    enabled: "&a[维护模式] &7服务器维护模式已启用！"
    disabled: "&c[维护模式] &7服务器维护模式已关闭！"
  
  # 踢出消息（多行）
  kick-message:
    - "&c&l⚠ 服务器维护中 ⚠"
    - ""
    - "&7服务器正在进行维护升级"
    - "&7请稍后再试！"
  
  # MOTD 显示
  motd:
    - "&c&l⚠ 服务器维护中 ⚠"
    - "&7正在进行维护，请稍后再试"
  
  # 版本栏文本
  version-text: "&c维护中 ✖"
  show-incompatible-version: true
  
  # 假玩家数
  show-fake-players: true
  fake-online: 0
  fake-max: 0
  
  # 悬浮提示
  hover-message:
    - "&c&l⚠ 服务器维护中 ⚠"
```

#### 权限说明

| 权限 | 说明 | 默认 |
|------|------|------|
| `tsl.maintenance.manage` | 管理维护模式 | OP |
| `tsl.maintenance.bypass` | 绕过维护模式限制 | OP |

---

### 3. 玩家体型调整 ⭐

允许玩家调整自己的体型大小，支持权限控制和范围限制。

**配置开关**：`scale.enabled`

#### 核心特性

- ✅ **体型调整** - 在配置的范围内调整体型
- ✅ **Bypass 权限** - OP 可突破范围限制
- ✅ **智能 Tab 补全** - 根据权限显示不同选项
- ✅ **体型重置** - 一键恢复默认体型
- ✅ **彩色消息** - 友好的提示信息

#### 使用方法

```bash
# 调整体型
/tsl scale 0.9        # 变小
/tsl scale 1.1        # 变大

# 重置体型
/tsl scale reset      # 恢复默认（1.0）

# OP/管理员（有 bypass 权限）
/tsl scale 0.5        # 可突破范围限制
/tsl scale 2.0        # 最大 2.0
```

#### 配置示例

**config.yml - scale 部分**
```yaml
scale:
  # 普通玩家的体型范围
  min: 0.8
  max: 1.1
  
  # 消息配置
  messages:
    prefix: "&c[TSL喵]&r "
    set: "%prefix%你的体型被调整为了 %scale%"
    reset: "%prefix%你的体型恢复默认啦"
    usage: "%prefix%用法: /tsl scale <数值|reset> (范围: %min% - %max%)"
    no-permission: "%prefix%你没有权限"
```

#### 权限说明

| 权限 | 说明 | 默认 |
|------|------|------|
| `tsl.scale.use` | 使用体型调整 | true |
| `tsl.scale.bypass` | 绕过范围限制（0.1-2.0） | OP |

#### Tab 补全行为

- **普通玩家**: 显示 `reset` 和配置范围内的值（如 0.8-1.1）
- **OP/管理员**: 显示 `reset` 和完整范围（0.1-2.0）

---

### 4. Hat 帽子系统 ⭐

将任意物品戴在头上，通过命令操作。

**配置开关**：`hat.enabled`

#### 核心特性

- ✅ **命令操作** - `/tsl hat` 将手持物品戴到头上
- ✅ **堆叠支持** - 手持多个物品时自动只戴1个
- ✅ **黑名单** - 防止特定物品被戴在头上
- ✅ **冷却时间** - 防止频繁使用
- ✅ **自动交换** - 原有头盔自动到手中或背包
- ✅ **Folia 兼容** - 使用实体调度器

#### 使用方法

```bash
# 将手中物品戴在头上
/tsl hat

# 示例1：手持1个钻石
/tsl hat  # 钻石戴到头上，原头盔到手中

# 示例2：手持64个钻石
/tsl hat  # 1个钻石戴到头上，手中剩63个
         # 如果原来有头盔，会尝试放回背包（满了掉地上）
```

#### 配置示例

**config.yml - hat 部分**
```yaml
hat:
  # 是否启用
  enabled: true
  
  # 冷却时间（秒），0表示无冷却
  cooldown: 0.0
  
  # 黑名单（禁止戴在头上的物品）
  blacklist:
    - COMMAND_BLOCK
  
  # 消息配置
  messages:
    prefix: "&c[Hat]&r "
    disabled: "%prefix%&cHat 功能已禁用"
    no-item: "%prefix%&c请先手持一个物品"
    blacklisted: "%prefix%&c该物品不能戴在头上"
    no-permission: "%prefix%&c你没有权限使用此命令"
    console-only: "%prefix%&c只有玩家才能使用此命令"
    cooldown: "%prefix%&c请等待 %cooldown% 秒后再使用"
```

#### 权限说明

| 权限 | 说明 | 默认 |
|------|------|------|
| `tsl.hat.use` | 使用 /tsl hat 命令 | true |

#### 使用场景

**场景 1：戴上单个物品**
1. 手持 1 个钻石
2. 执行 `/tsl hat`
3. 钻石戴到头上，原头盔到手中

**场景 2：手持堆叠物品**
1. 手持 64 个钻石
2. 执行 `/tsl hat`
3. 1个钻石戴到头上，手中剩63个
4. 原头盔自动放回背包（满了掉地上）

**场景 3：黑名单拦截**
1. 手持命令方块
2. 执行 `/tsl hat`
3. 提示：该物品不能戴在头上

---

### 5. 成就系统

自动隐藏普通成就的公屏消息，保留挑战成就，减少刷屏。

**配置开关**：`advancement.enabled`

// ...existing code...

---

### 6. 访客模式

为新玩家提供保护，怪物不会攻击访客，访客会发光。

**配置开关**：`visitor.enabled`

// ...existing code...

---

### 7. 权限检测器

基于 PlaceholderAPI 变量自动分配权限组。

**配置开关**：`permission-checker.enabled`

// ...existing code...

---

### 8. 农田保护

防止玩家和生物踩踏农田，保护农作物。

**配置开关**：`farmprotect.enabled`

// ...existing code...

---

### 9. MOTD 假玩家显示

在服务器列表中调整显示的在线人数。

**配置开关**：`fakeplayer.enabled`

#### 核心特性

- ✅ **智能过滤** - 隐藏普通成就，保留挑战成就
- ✅ **个人通知** - 玩家仍能看到自己的成就
- ✅ **成就统计** - 提供 PlaceholderAPI 占位符
- ✅ **手动刷新** - 可手动刷新统计数据

#### 使用方法

```bash
# 刷新成就统计
/tsl advcount refresh <玩家名>
/tsl advcount refresh all
```

#### PlaceholderAPI 占位符

- `%tsl_adv_count%` - 玩家的成就数量

---

### 5. 访客模式

为新玩家提供保护，怪物不会攻击访客，访客会发光。

#### 核心特性

- ✅ **怪物免疫** - 怪物不会主动攻击访客
- ✅ **发光效果** - 访客自动获得发光效果
- ✅ **权限驱动** - 基于 LuckPerms 权限实时生效
- ✅ **自定义提示** - 支持消息、标题、音效

#### 使用方法

```bash
# 给予访客权限
/lp user <玩家> permission set tsl.visitor true

# 移除访客权限
/lp user <玩家> permission unset tsl.visitor
```

#### 配置示例

**config.yml - visitor 部分**
```yaml
visitor:
  gained:
    chat: "&a[访客模式] &7你已进入访客模式！"
    title: "&a访客模式"
    subtitle: "&7已启用"
    sound: "entity.player.levelup"
  lost:
    chat: "&c[访客模式] &7你已退出访客模式！"
    title: "&c访客模式"
    subtitle: "&7已禁用"
    sound: "block.note_block.bass"
```

#### 权限说明

| 权限 | 说明 | 默认 |
|------|------|------|
| `tsl.visitor` | 访客权限 | false |

---

### 6. 权限检测器

基于 PlaceholderAPI 变量自动分配权限组。

#### 核心特性

- ✅ **自动检测** - 玩家加入时自动检测
- ✅ **变量驱动** - 支持任意 PlaceholderAPI 变量
- ✅ **自动分配** - 自动分配到目标权限组
- ✅ **命令执行** - 可选执行自定义命令

#### 配置示例

**config.yml - permission-checker 部分**
```yaml
permission-checker:
  enabled: true
  target-group: "normal"
  variable-name: "%player_gamemode%"
  variable-value: "SURVIVAL"
  execute-command: false
  commands:
    - "say 欢迎 %player% 加入服务器！"
```

---

### 7. 农田保护

防止玩家和生物踩踏农田，保护农作物。

#### 核心特性

- ✅ **自动保护** - 无需配置
- ✅ **全面防护** - 防止玩家和生物踩踏

---

### 8. MOTD 假玩家显示

在服务器列表中调整显示的在线人数。

#### 核心特性

- ✅ **人数调整** - 增加或减少显示人数
- ✅ **灵活配置** - 可为负数，最小显示 0
- ✅ **即时生效** - 重载后立即应用

#### 配置示例

**config.yml - fakeplayer 部分**
```yaml
fakeplayer:
  enabled: true      # 是否启用
  count: 3          # 显示人数 = 实际在线 + 3
```

---

## 配置文件详解

### 主配置文件：config.yml

插件的主要配置文件，包含所有功能模块的设置。

**版本控制**：
```yaml
config-version: 4
```
- 用于自动更新配置文件
- 插件更新时会自动添加新配置项
- 不会覆盖你的自定义配置

**功能开关**：
所有功能模块都有统一的开关格式：
```yaml
feature-name:
  enabled: true  # 默认启用，可单独禁用
```

支持的开关：
- `fakeplayer.enabled` - MOTD假玩家
- `alias.enabled` - 命令别名
- `advancement.enabled` - 成就消息过滤
- `farmprotect.enabled` - 农田保护
- `visitor.enabled` - 访客模式
- `permission-checker.enabled` - 权限检测
- `maintenance.enabled` - 维护模式
- `scale.enabled` - 体型调整
- `hat.enabled` - 帽子系统

**性能优化**：
- 开关状态在启动/reload时读取并缓存
- 事件处理直接读取缓存，零I/O开销
- 修改配置后执行 `/tsl reload` 立即生效

**完整配置示例**：参见 `src/main/resources/config.yml`

### 别名配置文件：aliases.yml

命令别名的配置文件，格式简单直观。

**格式**：
```yaml
aliases:
  - "别名:原命令"
  - "stp 子命令:原命令"
```

**示例**：
```yaml
aliases:
  - "t:tpa"
  - "传送:tpa"
  - "stp tp:teleport"
```

---

## 命令与权限

### 命令列表

| 命令 | 说明 | 权限 |
|------|------|------|
| `/tsl reload` | 重载所有配置 | `tsl.reload` (OP) |
| `/tsl aliasreload` | 重载别名配置 | `tsl.alias.reload` (OP) |
| `/tsl maintenance <on\|off>` | 切换维护模式 | `tsl.maintenance.manage` (OP) |
| `/tsl maintenance add <玩家>` | 添加白名单 | `tsl.maintenance.manage` (OP) |
| `/tsl maintenance remove <玩家>` | 移除白名单 | `tsl.maintenance.manage` (OP) |
| `/tsl maintenance whitelist` | 查看白名单 | `tsl.maintenance.manage` (OP) |
| `/tsl scale <数值\|reset>` | 调整体型 | `tsl.scale.use` (true) |
| `/tsl hat` | 将手持物品戴到头上 | `tsl.hat.use` (true) |
| `/tsl advcount refresh <玩家\|all>` | 刷新成就统计 | - |

### 权限列表

| 权限 | 说明 | 默认 |
|------|------|------|
| `tsl.reload` | 重载所有配置 | OP |
| `tsl.alias.reload` | 重载别名配置 | OP |
| `tsl.maintenance.manage` | 管理维护模式 | OP |
| `tsl.maintenance.bypass` | 绕过维护模式 | OP |
| `tsl.scale.use` | 使用体型调整 | true |
| `tsl.scale.bypass` | 绕过体型限制 | OP |
| `tsl.hat.use` | 使用Hat命令 | true |
| `tsl.visitor` | 访客权限 | false |

---

## PlaceholderAPI 扩展

### 安装 PlaceholderAPI

1. 下载 [PlaceholderAPI](https://www.spigotmc.org/resources/6245/)
2. 放入 `plugins` 目录
3. 重启服务器

### 可用占位符

| 占位符 | 说明 | 示例 |
|--------|------|------|
| `%tsl_adv_count%` | 玩家的成就数量 | 42 |

### 使用示例

**在其他插件中使用**：
```
你已完成 %tsl_adv_count% 个成就！
```

---

## 常见问题

### Q: 别名命令显示红色怎么办？
**A**: 新版本已修复此问题，别名会正确显示为白色。确保使用 `/tsl aliasreload` 重载配置。

### Q: 维护模式下 OP 也进不去？
**A**: 检查 OP 是否有 `tsl.maintenance.bypass` 权限。默认情况下，OP 应该自动拥有此权限。

### Q: PlaceholderAPI 占位符不工作？
**A**: 
1. 确保安装了 PlaceholderAPI 插件
2. 检查插件启动日志是否显示 "PlaceholderAPI 扩展已注册"
3. 使用 `/papi parse me %tsl_adv_count%` 测试

### Q: 访客模式不生效？
**A**: 
1. 确认玩家有 `tsl.visitor` 权限
2. 使用 `/lp user <玩家> permission set tsl.visitor true` 设置
3. 权限变更会实时生效，无需重新登录

### Q: 如何临时关闭某个功能？
**A**: 编辑 `config.yml` 中对应功能的 `enabled` 选项（部分功能支持），然后执行 `/tsl reload`。

### Q: 配置文件更新后会覆盖我的设置吗？
**A**: 不会！插件有智能配置更新系统：
- 保留所有现有配置的值
- 只添加新版本中新增的配置项
- 自动备份到 `.backup` 文件

### Q: 体型设置会保存吗？
**A**: 不会持久化。玩家重新登录后体型会恢复默认值（1.0）。

---

## 二次开发指南

### 开发环境

- **IDE**: IntelliJ IDEA 2024+
- **JDK**: Java 21
- **Kotlin**: 1.9.21
- **Gradle**: 8.5
- **服务器**: Paper/Folia 1.21.8

### 克隆与构建

```bash
# 克隆项目
git clone <repository-url>
cd TSLplugins

# 构建插件
./gradlew clean build

# 生成 shadowJar（包含依赖）
./gradlew shadowJar

# 输出位置
# build/libs/TSLplugins-1.0.jar
```

### 项目结构

```
src/main/kotlin/org/tsl/tSLplugins/
├── TSLplugins.kt              # 主插件类
├── TSLCommand.kt              # 统一命令分发器
├── ReloadCommand.kt           # 重载命令处理
├── ConfigUpdateManager.kt     # 配置更新管理
├── Alias/                     # 命令别名系统
│   ├── AliasManager.kt
│   ├── DynamicAliasCommand.kt
│   └── AliasCommand.kt
├── Maintenance/               # 维护模式系统
│   ├── MaintenanceManager.kt
│   ├── MaintenanceCommand.kt
│   ├── MaintenanceLoginListener.kt
│   ├── MaintenanceMotdListener.kt
│   └── MaintenancePermissionListener.kt
├── Scale/                     # 体型调整系统
│   ├── ScaleManager.kt
│   └── ScaleCommand.kt
├── Hat/                       # Hat帽子系统
│   ├── HatManager.kt
│   └── HatCommand.kt
├── Advancement/               # 成就系统
│   ├── AdvancementMessage.kt
│   ├── AdvancementCount.kt
│   ├── AdvancementCommand.kt
│   └── TSLPlaceholderExpansion.kt
├── Visitor/                   # 访客系统
│   └── VisitorEffect.kt
├── Permission/                # 权限检测
│   └── PermissionChecker.kt
├── Farmprotect/              # 农田保护
│   └── FarmProtect.kt
└── Motd/                     # MOTD 模块
    └── FakePlayerMotd.kt
```

### 添加新功能模块

1. **创建新包**
   ```kotlin
   src/main/kotlin/org/tsl/tSLplugins/YourFeature/
   ```

2. **实现功能类**
   ```kotlin
   class YourFeature(private val plugin: TSLplugins) : Listener {
       
       fun onEnable() {
           plugin.server.pluginManager.registerEvents(this, plugin)
       }
       
       fun reload() {
           // 重载逻辑
       }
   }
   ```

3. **注册到主插件**
   ```kotlin
   // 在 TSLplugins.kt 中
   private lateinit var yourFeature: YourFeature
   
   override fun onEnable() {
       yourFeature = YourFeature(this)
       yourFeature.onEnable()
   }
   ```

4. **添加配置项**（可选）
   ```yaml
   # 在 config.yml 中
   your-feature:
     enabled: true
     option: "value"
   ```

5. **更新配置版本**
   ```kotlin
   // 在 ConfigUpdateManager.kt 中
   const val CURRENT_CONFIG_VERSION = 4  // 递增版本号
   ```

### 代码规范

- 类名使用 **PascalCase**：`MyFeatureManager`
- 函数名使用 **camelCase**：`loadConfig()`
- 私有字段使用 **camelCase**：`private val configFile`
- 常量使用 **UPPER_SNAKE_CASE**：`const val MAX_VALUE = 100`
- 使用 Kotlin 空安全特性：`?.` 和 `?:`
- 优先使用数据类：`data class Config(...)`
- 使用字符串模板：`"玩家 $name 已加入"`

### Folia 兼容性注意事项

使用 Folia 时需要注意：

1. **实体相关操作使用实体调度器**：
   ```kotlin
   player.scheduler.run(plugin, { _ ->
       // 实体操作
   }, null)
   ```

2. **定时任务使用区域调度器**：
   ```kotlin
   plugin.server.globalRegionScheduler.runAtFixedRate(plugin, { _ ->
       // 定时任务
   }, 20L, 20L)
   ```

3. **避免跨区域操作**：
   - 不要在一个区域访问另一个区域的实体
   - 使用消息队列或调度器传递数据

### 依赖管理

**build.gradle.kts**：
```kotlin
dependencies {
    compileOnly("io.papermc.paper:paper-api:1.21.8-R0.1-SNAPSHOT")
    compileOnly("net.luckperms:api:5.4")
    compileOnly("me.clip:placeholderapi:2.11.6")
    compileOnly("com.comphenix.protocol:ProtocolLib:5.3.0")
    
    implementation("org.jetbrains.kotlin:kotlin-stdlib-jdk8")
}
```

### 调试技巧

1. **使用日志**：
   ```kotlin
   plugin.logger.info("信息日志")
   plugin.logger.warning("警告日志")
   plugin.logger.severe("严重错误")
   ```

2. **配置验证**：
   ```kotlin
   if (!config.contains("your-feature.enabled")) {
       logger.warning("配置项缺失，使用默认值")
   }
   ```

3. **异常处理**：
   ```kotlin
   try {
       // 可能出错的代码
   } catch (e: Exception) {
       logger.severe("发生错误: ${e.message}")
       e.printStackTrace()
   }
   ```

### 测试建议

1. **单元测试** - 测试独立功能
2. **集成测试** - 测试模块间交互
3. **服务器测试** - 在测试服务器中实际运行
4. **Folia 测试** - 在 Folia 服务器上测试兼容性

---

## 更新日志

### v1.0 (2025-11-11)
- ✅ 命令别名系统（支持 Tab 补全）
- ✅ 维护模式系统（白名单、MOTD）
- ✅ 玩家体型调整（Bypass 权限）
- ✅ Hat 帽子系统（命令+背包操作）
- ✅ 成就系统（智能过滤）
- ✅ 访客模式（怪物免疫、发光）
- ✅ 权限检测器（自动分配）
- ✅ 农田保护
- ✅ MOTD 假玩家显示
- ✅ 完全支持 Folia 1.21.8
- ✅ 智能配置更新系统

---

## 技术支持

如有问题或建议，请联系插件开发者或提交 Issue。

**感谢使用 TSLplugins！** 🎉

---

**最后更新**: 2025-11-11  
**文档版本**: 1.0

