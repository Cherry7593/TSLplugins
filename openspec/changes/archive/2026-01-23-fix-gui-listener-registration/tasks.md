# Tasks: Fix GUI Listener Registration

## 实施任务

- [x] T1. TownPHomeModule 注册 GUI 监听器
  - 在 `doEnable()` 中添加 `registerListener(gui)` ✅

- [x] T2. McediaModule 注册 GUI 监听器
  - 在 `doEnable()` 中添加 `registerListener(gui)` ✅

- [x] T3. 验证构建
  - 运行 `./gradlew classes` ✅
